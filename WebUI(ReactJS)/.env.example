# Mapper WebUI (ReactJS) - Environment Variables
# Copy this file to .env and adjust values for your environment.
# IMPORTANT: All variables must be prefixed with REACT_APP_ to be exposed to the client app.

# Base URL for backend REST API (no trailing slash). Defaults to http://localhost:8000/api
REACT_APP_API_URL=http://localhost:8000/api

# Base URL for WebSocket server (ws or wss) used for notifications.
# Example: ws://localhost:8000/ws
REACT_APP_WS_URL=ws://localhost:8000/ws

# Site URL used for redirects in auth flows (e.g., email verification, password reset)
REACT_APP_SITE_URL=http://localhost:3000

# Cypress base URL for E2E tests. If not set, defaults to http://localhost:3000 (see cypress.config.ts)
# CYPRESS_BASE_URL=http://localhost:3000

# Optional: Feature flags
# REACT_APP_FEATURE_FLAGS={}

# Optional: Build-time metadata
# REACT_APP_BUILD_VERSION=local
# REACT_APP_BUILD_TIMESTAMP=$(date -u +%Y-%m-%dT%H:%M:%SZ)
